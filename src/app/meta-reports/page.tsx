import NotFoundPage from '@/components/NotFoundPage';

export default function MetaReportsPage() {
  return (
    <NotFoundPage 
      message="META REPORTS OFFLINE"
      subtitle="Cross-signal pattern analysis is being calibrated. This section will aggregate monthly trends."
    />
  );
}