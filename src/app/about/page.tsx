import NotFoundPage from '@/components/NotFoundPage';

export default function AboutPage() {
  return (
    <NotFoundPage 
      message="ABOUT TRANSMISSION PENDING"
      subtitle="Stay tuned for mission details."
    />
  );
}