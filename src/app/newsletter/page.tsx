import NotFoundPage from '@/components/NotFoundPage';

export default function NewsletterPage() {
  return (
    <NotFoundPage 
      message="NEWSLETTER FREQUENCY INACTIVE"
      subtitle={
        "Direct subscription interface is being configured. Weekly briefings launching soon."
      }
    />
  );
}